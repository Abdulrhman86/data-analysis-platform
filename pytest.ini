[pytest]
testpaths = app/tests
filterwarnings =
    # matplotlib drags in pyparsing deprecation warnings via one export-sanitizer test
    ignore:.*deprecated.*use.*:DeprecationWarning
    # expected for the all-NaN data-quality edge-case test
    ignore:Mean of empty slice:RuntimeWarning
