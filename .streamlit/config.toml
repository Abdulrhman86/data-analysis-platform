# Streamlit configuration (read from the repo root on Streamlit Community Cloud).
[server]
maxUploadSize = 50
headless = true

[browser]
gatherUsageStats = false

[theme]
base = "dark"
primaryColor = "#6366f1"
backgroundColor = "#0b1020"
secondaryBackgroundColor = "#131a2e"
textColor = "#f8fafc"
